name: nexus_ai
publish_to: "none"
description: NEXUS AI - Android and Windows client
version: 2.3.0+4

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.3.2
  url_launcher: ^6.3.1
  file_picker: ^8.1.7
  video_player: ^2.9.2

flutter:
  uses-material-design: true


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
