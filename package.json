{
  "name": "nexus-ai",
  "version": "2.0.0",
  "private": true,
  "description": "NEXUS AI — GitHub source control and Cloudflare runtime.",
  "scripts": {
    "check": "npm --prefix cloudflare install --no-audit --no-fund && npm --prefix cloudflare run check",
    "deploy": "npm --prefix cloudflare run deploy",
    "dev": "npm --prefix cloudflare run dev"
  }
}
